{
  "name": "bridgeagent-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal real backend for BridgeAgent: provisions a Dial number, sends SMS, and places calls via the Dial API.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  }
}
